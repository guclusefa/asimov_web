module.exports = {
    // affichage accueil ----------------------------------------------------------------------------------------------
    afficher: function (req, res) {
        titre = "Accueil";
        res.json({ titre, valid, erreur, user_info, })
    },
}